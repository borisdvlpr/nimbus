apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Reconciled by the `apps` Flux Kustomization (clusters/nimbus/apps.yaml), which
# depends on infra-controllers + infra-storage and has SOPS decryption wired.
resources:
  - namespace.yaml
  - valkey
  - web
