apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Placeholder — reconciles green with nothing to apply.
# Increment 5 adds: tuned Postgres + a custom multi-arch web app, exposed via Traefik.
resources: []
