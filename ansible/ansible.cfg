[defaults]
inventory = ./inventory.ini
roles_path = ./roles
# Tailscale SSH presents its own host keys; skip interactive verification for automation.
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
interpreter_python = auto_silent
gathering = smart

[ssh_connection]
pipelining = True
